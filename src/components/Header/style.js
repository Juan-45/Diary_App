import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  headerText: {
    fontFamily: 'Roboto',
    fontStyle: 'italic',
    fontWeight: 'bold',
  },
  header: {
    paddingTop: '25px',
    paddingBottom: '25px',
  },
});

export { useStyles };
